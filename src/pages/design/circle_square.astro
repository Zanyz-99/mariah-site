---
import Layout from "../../layouts/Layout.astro";
import { promises as fs } from "node:fs";
import path from "node:path";

const FOLDER = "Circle_Square";
const BASE = `/design/${FOLDER}`;
const PUBLIC_DIR = path.join(process.cwd(), "public", "design", FOLDER);

const txtFile = "circle_square.txt";
const subtitle = "Scandinavian Furniture Design Studio";

const isImage = (name: string) => /\.(png|jpe?g|webp|avif|gif|svg)$/i.test(name);

let descriptionText = "";
let imagesInFolder: string[] = [];

try {
  descriptionText = await fs.readFile(path.join(PUBLIC_DIR, txtFile), "utf-8");

  const entries = await fs.readdir(PUBLIC_DIR, { withFileTypes: true });
  imagesInFolder = entries
    .filter((e) => e.isFile() && isImage(e.name))
    .map((e) => e.name);
} catch (err) {
  console.error("[Circle Square page] asset read error:", err);
}

// Ordered Images 01–05
const orderedFiles = [
  "Images-02.webp",
  "Images-03.webp",
  "Images-04.webp",
  "cover-image.webp",
];

// Case-insensitive match
const byLower = new Map(imagesInFolder.map((n) => [n.toLowerCase(), n]));
const finalFiles = orderedFiles
  .map((n) => byLower.get(n.toLowerCase()))
  .filter(Boolean) as string[];

const galleryImages = finalFiles.map((name) => `${BASE}/${encodeURIComponent(name)}`);

const coverIndex = finalFiles.findIndex((f) => f.toLowerCase() === "cover-image.webp");
const coverSrc = coverIndex >= 0 ? galleryImages[coverIndex] : `${BASE}/cover-image.webp`;

const nonCover = finalFiles
  .map((file, i) => ({ file, src: galleryImages[i], i }))
  .filter((x) => x.file.toLowerCase() !== "cover-image.webp");
---

<Layout title="Circle Within a Square" description="Circle Within a Square project">
  <main class="circle_square">
    <section class="circle_square__hero" aria-label="Circle Square hero">
      <div class="circle_square__inner">

        <figure class="circle_square__cover" id="coverFigure">
          <button
            class="circle_square__imgBtn"
            type="button"
            data-lb-index={String(coverIndex >= 0 ? coverIndex : 0)}
            aria-label="Open cover image"
          >
            <img class="circle_square__img--cover" src={coverSrc} alt="Cover image" loading="eager" decoding="async" />
          </button>

          <div id="coverEnd" aria-hidden="true"></div>
        </figure>

        <header class="circle_square__meta">
          <h1 class="circle_square__title">Circle Within a Square</h1>
          <div class="circle_square__subtitle">{subtitle}</div>
        </header>

        <div class="circle_square__heroSpacer" aria-hidden="true"></div>
      </div>
    </section>

    <section class="circle_square__desc" aria-label="Project description">
      <div class="circle_square__inner">
        {descriptionText ? (
          <div class="circle_square__body">{descriptionText}</div>
        ) : (
          <div class="circle_square__body circle_square__body--error">
            Couldn’t load <code>circle_square.txt</code><br />
            Expected: <code>public/design/{FOLDER}/circle_square.txt</code>
          </div>
        )}
      </div>
    </section>

    <section class="circle_square__gallery" aria-label="Circle Square gallery">
      <div class="circle_square__inner">
        <div class="circle_square__grid circle_square__grid--gallery">
          {nonCover.map(({ src, i }) => (
            <figure class="circle_square__galleryItem">
              <button class="circle_square__imgBtn circle_square__imgBtn--full" type="button" data-lb-index={String(i)} aria-label="Open image">
                <img class="circle_square__img--full" src={src} alt="" loading="lazy" decoding="async" />
              </button>
            </figure>
          ))}
        </div>
      </div>
    </section>

    <div id="footerTrigger" aria-hidden="true"></div>

    <!-- Lightbox -->
    <div class="lb" id="lightbox" aria-hidden="true">
      <div class="lb__backdrop" data-lb-close></div>

      <div class="lb__panel" role="dialog" aria-modal="true" aria-label="Image viewer">
        <button class="lb__close" type="button" data-lb-close aria-label="Close">×</button>
        <button class="lb__nav lb__nav--prev" type="button" data-lb-prev aria-label="Previous">‹</button>

        <figure class="lb__figure">
          <div class="lb__stage">
            <img class="lb__img" id="lbImg" alt="" />
          </div>
        </figure>

        <button class="lb__nav lb__nav--next" type="button" data-lb-next aria-label="Next">›</button>
      </div>
    </div>

    <!-- Lightbox behavior -->
    <script define:vars={{ galleryImages }} is:inline>
      (function () {
        const images = galleryImages;
        const lb = document.getElementById("lightbox");
        const lbImg = document.getElementById("lbImg");

        let index = 0;
        let lastFocus = null;

        const isOpen = () => lb.getAttribute("aria-hidden") === "false";

        const setOpen = (open) => {
          lb.setAttribute("aria-hidden", open ? "false" : "true");
          document.documentElement.classList.toggle("lb-open", open);
          document.body.classList.toggle("lb-open", open);
        };

        const show = (i) => {
          index = (i + images.length) % images.length;
          lbImg.src = images[index];
        };

        const open = (i) => {
          lastFocus = document.activeElement;
          show(i);
          setOpen(true);
          const closeBtn = lb.querySelector(".lb__close");
          closeBtn && closeBtn.focus();
        };

        const close = () => {
          setOpen(false);
          lbImg.removeAttribute("src");
          if (lastFocus && typeof lastFocus.focus === "function") lastFocus.focus();
        };

        const next = () => show(index + 1);
        const prev = () => show(index - 1);

        document.addEventListener("click", (e) => {
          const t = e.target;

          const btn = t.closest?.("[data-lb-index]");
          if (btn) {
            const i = Number(btn.getAttribute("data-lb-index") || 0);
            if (!Number.isNaN(i)) open(i);
            return;
          }

          if (t.closest?.("[data-lb-close]")) return close();
          if (t.closest?.("[data-lb-next]")) return next();
          if (t.closest?.("[data-lb-prev]")) return prev();
        });

        document.addEventListener("keydown", (e) => {
          if (!isOpen()) return;
          if (e.key === "Escape") close();
          if (e.key === "ArrowRight") next();
          if (e.key === "ArrowLeft") prev();
        });
      })();
    </script>

    <!-- ✅ Background + nav style flip when cover image is fully off-screen -->
    <script is:inline>
      (function () {
        const end = document.getElementById("coverEnd");
        if (!end) return;

        const nav =
          document.querySelector("nav.topbar") ||
          document.querySelector("nav") ||
          document.querySelector("header nav");

        const setWhiteMode = (on) => {
          document.documentElement.classList.toggle("circle_square-white", on);
          document.body.classList.toggle("circle_square-white", on);
          if (nav) nav.classList.toggle("circle_square-white", on);
        };

        const update = () => {
          const r = end.getBoundingClientRect();
          // Flip once cover is completely off the viewport
          setWhiteMode(r.top <= 0);
        };

        const io = new IntersectionObserver(() => update(), { threshold: [0, 1] });
        io.observe(end);

        window.addEventListener("scroll", update, { passive: true });
        window.addEventListener("resize", update);
        requestAnimationFrame(update);
      })();
    </script>

    <!-- Footer turns white ONLY when you're at the bottom -->
    <script is:inline>
      (function () {
        const footer = document.querySelector("footer");
        const trigger = document.getElementById("footerTrigger");
        if (!footer || !trigger) return;

        const setFooterWhite = (on) => footer.classList.toggle("circle_square-footer-white", on);

        const io = new IntersectionObserver(([entry]) => setFooterWhite(entry.isIntersecting), { threshold: 0.01 });
        io.observe(trigger);
      })();
    </script>

    <style>
      /* --- Page-specific overrides: make Layout footer NOT fixed here --- */
      :global(footer) {
        position: static !important;
        left: auto !important;
        right: auto !important;
        bottom: auto !important;
        backdrop-filter: none !important;
        -webkit-backdrop-filter: none !important;
        background: transparent !important;
        border-top: none !important;
      }

      :global(main) {
        padding-bottom: 0 !important;
      }

      /* Ensure navbar scrolls normally + no border */
      :global(nav),
      :global(nav.topbar) {
        position: static !important;
        top: auto !important;
        left: auto !important;
        right: auto !important;
        z-index: auto !important;
        border-bottom: none !important;
        box-shadow: none !important;
      }

      :global(nav.topbar .row),
      :global(nav .row) {
        border-bottom: none !important;
      }

      /* ---------------------------
         FULL PAGE background + variable text colors
         --------------------------- */
      :global(html),
      :global(body) {
        background: #121212 !important;
        transition: background 200ms ease;
      }

      :global(html) {
        --circle_square-fg: #ffffff;
        --circle_square-muted: rgba(255, 255, 255, 0.74);
        --circle_square-body: rgba(255, 255, 255, 0.78);
        --circle_square-border: rgba(255, 255, 255, 0.16);
      }

      :global(html.circle_square-white),
      :global(body.circle_square-white) {
        background: #ffffff !important;
      }

      :global(html.circle_square-white) {
        --circle_square-fg: #0b0b0b;
        --circle_square-muted: rgba(0, 0, 0, 0.62);
        --circle_square-body: rgba(0, 0, 0, 0.72);
        --circle_square-border: rgba(0, 0, 0, 0.12);
      }

      /* Keep navbar borderless in both states */
      :global(nav.circle_square-white),
      :global(nav.topbar.circle_square-white) {
        border-bottom: none !important;
        box-shadow: none !important;
      }

      /* Footer only becomes white when you're at bottom */
      :global(footer.circle_square-footer-white) {
        background: #ffffff !important;
        border-top: 1px solid rgba(0, 0, 0, 0.12) !important;
        color: #0b0b0b !important;
      }

      :global(footer.circle_square-footer-white a) {
        color: #0b0b0b !important;
      }

      /* ---------------------------
         PAGE WIDTH: match index “window/card” feel
         --------------------------- */
      .circle_square {
        width: 100%;
        margin: 0;
        padding-top: 2rem;
      }

      .circle_square__inner {
        width: min(100vw, clamp(320px, 78vw, 980px));
        margin-inline: auto;
      }

      /* ---------------------------
         HERO (no description visible until scroll)
         --------------------------- */
      .circle_square__hero {
        min-height: calc(100svh - 2rem);
        display: block;
        padding-bottom: 0;
      }

      .circle_square__inner {
        display: flex;
        flex-direction: column;
      }

      .circle_square__cover {
        margin: 0;
        border-radius: 0px;
        overflow: hidden;
        aspect-ratio: 16 / 9;
        border: 1px solid var(--circle_square-border);
        background: transparent;
      }

      .circle_square__imgBtn {
        display: block;
        width: 100%;
        height: 100%;
        padding: 0;
        border: 0;
        background: transparent;
        cursor: zoom-in;
      }

      .circle_square__img--cover {
        width: 100%;
        height: 100%;
        display: block;
        object-fit: cover;
        object-position: center;
        user-select: none;
        -webkit-user-drag: none;
      }

      #coverEnd {
        height: 1px;
        width: 100%;
      }

      .circle_square__meta {
        margin-top: 1.05rem;
      }

      .circle_square__title {
        margin: 0 0 0.45rem;
        font-size: clamp(1.6rem, 2.2vw, 2.15rem);
        letter-spacing: -0.02em;
        color: var(--circle_square-fg);
      }

      .circle_square__subtitle {
        font-size: 0.95rem;
        font-weight: 500;
        letter-spacing: 0.02em;
        color: var(--circle_square-muted);
      }

      .circle_square__heroSpacer {
        flex: 1 1 auto;
        min-height: 24px;
      }

      /* ---------------------------
         DESCRIPTION (appears on scroll)
         --------------------------- */
      .circle_square__desc {
        padding: 0 0 2.25rem;
      }

      /* Re-declare inner as block for non-hero sections */
      .circle_square__desc .circle_square__inner,
      .circle_square__gallery .circle_square__inner {
        display: block;
      }

      .circle_square__body {
        white-space: pre-line;
        line-height: 1.65;
        font-size: 1rem;
        color: var(--circle_square-body);
      }

      .circle_square__body--error {
        opacity: 0.95;
      }

      /* ---------------------------
         GALLERY
         --------------------------- */
      .circle_square__gallery {
        padding: 1.5rem 0 3rem;
      }

      .circle_square__grid {
        display: grid;
      }

      .circle_square__grid--gallery {
        grid-template-columns: 1fr;
        gap: 1.4rem;
      }

      .circle_square__galleryItem {
        margin: 0;
        padding: 0;
        background: transparent;
        border: 0;
      }

      .circle_square__imgBtn--full {
        width: 100%;
        height: auto;
        padding: 0.75rem 0;
      }

      .circle_square__img--full {
        display: block;
        width: auto;
        height: auto;
        max-width: 100%;
        margin: 0 auto;
        object-fit: contain;
      }

      #footerTrigger {
        height: 1px;
        width: 100%;
      }

      /* ---------------------------
         Lightbox
         --------------------------- */
      .lb {
        position: fixed;
        inset: 0;
        display: none;
        z-index: 9999;
      }
      .lb[aria-hidden="false"] {
        display: block;
      }

      .lb__backdrop {
        position: absolute;
        inset: 0;
        background: #fff;
      }

      .lb__panel {
        position: absolute;
        inset: 0;
        display: grid;
        grid-template-columns: 56px 1fr 56px;
        align-items: center;
        padding: 2rem 1rem;
      }

      .lb__figure {
        margin: 0;
        height: 100%;
        display: grid;
        place-items: center;
      }

      .lb__stage {
        background: #fff;
        width: min(1100px, 92vw);
        height: 86vh;
        display: grid;
        place-items: center;
      }

      .lb__img {
        max-width: 100%;
        max-height: 100%;
        object-fit: contain;
        border-radius: 0;
        box-shadow: 0 12px 40px rgba(0, 0, 0, 0.2);
      }

      .lb__close {
        position: absolute;
        top: 18px;
        right: 18px;
        width: 44px;
        height: 44px;
        border: 1px solid rgba(0, 0, 0, 0.15);
        background: rgba(255, 255, 255, 0.9);
        color: #111;
        font-size: 28px;
        line-height: 40px;
        border-radius: 999px;
        cursor: pointer;
      }

      .lb__nav {
        width: 44px;
        height: 64px;
        border: 1px solid rgba(0, 0, 0, 0.15);
        background: rgba(255, 255, 255, 0.9);
        color: #111;
        font-size: 40px;
        border-radius: 10px;
        cursor: pointer;
        user-select: none;
      }

      :global(html.lb-open),
      :global(body.lb-open) {
        overflow: hidden;
      }

      /* ---------------------------
         Mobile
         --------------------------- */
      @media (max-width: 640px) {
        .circle_square__inner {
          width: 100vw;
          padding-inline: 4vw;
        }

        .circle_square__cover {
          aspect-ratio: 9 / 16;
        }
      }

      @media (prefers-reduced-motion: reduce) {
        :global(html),
        :global(body) {
          transition: none !important;
        }
      }
    </style>

  </main>
</Layout>
