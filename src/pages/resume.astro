---
import Layout from "../layouts/Layout.astro";
---

<Layout title="About" description="Background, experience, and interests.">
  <section class="content about-wrap">
    <div class="spacer-md" aria-hidden="true"></div>

    <!-- Accordion -->
    <section class="accordion" aria-label="About sections">

      <!-- EDUCATION -->
      <div class="acc" id="acc-education">
        <button class="acc-summary" type="button" aria-expanded="false" aria-controls="panel-education" id="btn-education">
          <span class="acc-toggle" aria-hidden="true"></span>
          <span class="acc-title">Education</span>
        </button>
        <div class="acc-panel" id="panel-education" role="region" aria-labelledby="btn-education">
          <div class="block">
            <div class="edu">
              <div class="edu-item">
                <div class="edu-top">
                  <div class="edu-school">University of Washington</div>
                  <div class="edu-dates">2021–2024</div>
                </div>
                <div class="edu-sub">College of Built Environments</div>
                <ul class="bullets">
                  <li>Master of Architecture</li>
                  <li>Certificate of Historic Preservation</li>
                </ul>
              </div>

              <div class="edu-item">
                <div class="edu-top">
                  <div class="edu-school">Boston University</div>
                  <div class="edu-dates">2013–2017</div>
                </div>
                <div class="edu-sub">College of Fine Arts</div>
                <ul class="bullets">
                  <li>BFA Graphic Design</li>
                  <li>Cum Laude</li>
                </ul>
              </div>
            </div>
          </div>
        </div>
      </div>

      <!-- SKILLS -->
      <div class="acc" id="acc-skills">
        <button class="acc-summary" type="button" aria-expanded="false" aria-controls="panel-skills" id="btn-skills">
          <span class="acc-toggle" aria-hidden="true"></span>
          <span class="acc-title">Skills</span>
        </button>
        <div class="acc-panel" id="panel-skills" role="region" aria-labelledby="btn-skills">
          <div class="block">
            <div class="chips" aria-label="Skills list">
              <span class="chip">Adobe Suite</span>
              <span class="chip">Rhino 7</span>
              <span class="chip">Revit</span>
              <span class="chip">Enscape / Lumion</span>
              <span class="chip">Microsoft Office</span>
              <span class="chip">3D printing</span>
              <span class="chip">Laser Cutting</span>
              <span class="chip">CNC Milling</span>
              <span class="chip">Woodworking</span>
              <span class="chip">TIG Welding</span>
              <span class="chip">Knitting</span>
              <span class="chip">Sewing</span>
              <span class="chip">Needlepoint</span>
            </div>
          </div>
        </div>
      </div>

      <!-- ACHIEVEMENTS / AWARDS -->
      <div class="acc" id="acc-achievements">
        <button class="acc-summary" type="button" aria-expanded="false" aria-controls="panel-achievements" id="btn-achievements">
          <span class="acc-toggle" aria-hidden="true"></span>
          <span class="acc-title">Achievements / Awards</span>
        </button>
        <div class="acc-panel" id="panel-achievements" role="region" aria-labelledby="btn-achievements">
          <div class="block">
            <ul class="bullets">
              <li>Secretary, University of Washington College of Built Environments Happy Hour</li>
              <li>Youths in the Built Environments — Volunteer</li>
              <li>Captain, Boston University Lightweight Women’s Rowing</li>
              <li>BU Athletics Leadership Academy</li>
            </ul>
          </div>
        </div>
      </div>

      <!-- INTERESTS / HOBBIES -->
      <div class="acc" id="acc-interests">
        <button class="acc-summary" type="button" aria-expanded="false" aria-controls="panel-interests" id="btn-interests">
          <span class="acc-toggle" aria-hidden="true"></span>
          <span class="acc-title">Interests / Hobbies</span>
        </button>
        <div class="acc-panel" id="panel-interests" role="region" aria-labelledby="btn-interests">
          <div class="block">
            <ul class="bullets">
              <li>Building / Creating</li>
              <li>Hiking / Camping</li>
              <li>Traveling / Exploring</li>
            </ul>
          </div>
        </div>
      </div>

      <!-- WORK EXPERIENCE -->
      <div class="acc" id="acc-work">
        <button class="acc-summary" type="button" aria-expanded="false" aria-controls="panel-work" id="btn-work">
          <span class="acc-toggle" aria-hidden="true"></span>
          <span class="acc-title">Work</span>
        </button>
        <div class="acc-panel" id="panel-work" role="region" aria-labelledby="btn-work">
          <div class="list dim-others" aria-label="Work history">

            <!-- Miller Hull -->
            <div class="row hoverable">
              <div class="row-line no-logo">
                <div class="meta">
                  <div class="title">Architecture Intern / Project Architect</div>
                  <div class="sub">Miller Hull · Seattle, WA</div>
                  <div class="sub">June 2022 – September 2023</div>
                  <div class="desc">
                    Initially hired as a summer architecture intern; responsibilities included rendering development,
                    architectural graphics for award submissions, geographic and historic site research, and physical
                    model making. Hired as a regular employee after the internship and continued part-time through
                    the school year. In summer 2023, contracted to Olson Kundig (remained a Miller Hull employee),
                    focusing on renderings, site/material research, presentation design, and floor plan schemes.
                    Due to contract agreements, rendering examples from Olson Kundig work are not available.
                  </div>
                </div>
              </div>
            </div>

            <!-- UW Reader/Grader -->
            <div class="row hoverable">
              <div class="row-line no-logo">
                <div class="meta">
                  <div class="title">ARCH 151 Reader / Grader</div>
                  <div class="sub">University of Washington · Seattle, WA</div>
                  <div class="sub">March 2024 – June 2024</div>
                  <div class="desc">
                    Supported course instruction through assignment evaluation and feedback; coordinated grading
                    workflow and maintained consistency across rubric-based assessments.
                  </div>
                </div>
              </div>
            </div>

            <!-- Interior Showplace -->
            <div class="row hoverable">
              <div class="row-line no-logo">
                <div class="meta">
                  <div class="title">Project Coordinator</div>
                  <div class="sub">Interior Showplace · Honolulu, HI</div>
                  <div class="sub">March 2021 – August 2021</div>
                  <div class="desc">
                    Coordinated private and government bids involving interior design services and furniture purchases
                    across Oahu, Maui, Hawai’i, and Kaua’i. Supported early design services through furniture research
                    and quote-building with designers; placed and tracked orders; coordinated delivery and installation.
                    Projects ranged from $1,000 to $700,000. Designed presentation templates and marketing materials
                    to support the sales team.
                  </div>
                </div>
              </div>
            </div>

            <!-- Novus Architects -->
            <div class="row hoverable">
              <div class="row-line no-logo">
                <div class="meta">
                  <div class="title">Marketing and Graphics Coordinator</div>
                  <div class="sub">Novus Architects, Inc. · Charleston, SC</div>
                  <div class="sub">September 2019 – February 2021</div>
                  <div class="desc">
                    Supported Business Development with research and evaluation of RFPs/RFQs and created proposal
                    submissions for public and private bids. Designed print and digital marketing materials (brochures,
                    infographics, postcards, presentations). Coordinated project photography, developed social media
                    campaigns, updated the website, and supported environmental graphics efforts. Reported to the
                    Director of Business Development; led weekly marketing meetings, presented updates in bi-weekly
                    all-office meetings, and trained/managed a marketing intern.
                  </div>
                </div>
              </div>
            </div>

            <!-- Mum's Place -->
            <div class="row hoverable">
              <div class="row-line no-logo">
                <div class="meta">
                  <div class="title">Marketing Coordinator</div>
                  <div class="sub">Mum’s Place · Monterey, CA</div>
                  <div class="sub">September 2018 – July 2019</div>
                  <div class="desc">
                    Focused on website optimization, social media maintenance, branding development, and advertising
                    coordination. Occasionally supported interior designers with finish selections, proposal coordination,
                    and client communications.
                  </div>
                </div>
              </div>
            </div>

            <!-- Service / Hospitality (gaps) -->
            <div class="row hoverable">
              <div class="row-line no-logo">
                <div class="meta">
                  <div class="title">Bartender</div>
                  <div class="sub">Métier Brewing Co. · Seattle, WA</div>
                  <div class="sub">April 2022 – January 2024</div>
                  <div class="desc">
                    Managed high-volume service, maintained bar operations and inventory, and supported a welcoming
                    guest experience.
                  </div>
                </div>
              </div>
            </div>

            <div class="row hoverable">
              <div class="row-line no-logo">
                <div class="meta">
                  <div class="title">Bartender</div>
                  <div class="sub">Optimism Brewery · Seattle, WA</div>
                  <div class="sub">September 2021 – April 2022</div>
                  <div class="desc">
                    Provided fast-paced service and coordinated day-to-day bar setup/close while maintaining quality
                    and consistency.
                  </div>
                </div>
              </div>
            </div>

            <div class="row hoverable">
              <div class="row-line no-logo">
                <div class="meta">
                  <div class="title">Server</div>
                  <div class="sub">Island Vintage Wine Bar · Waikiki, HI</div>
                  <div class="sub">April 2021 – August 2021</div>
                  <div class="desc">
                    Delivered attentive table service in a high-traffic environment; coordinated timing and communication
                    between guests, bar, and kitchen.
                  </div>
                </div>
              </div>
            </div>

            <div class="row hoverable">
              <div class="row-line no-logo">
                <div class="meta">
                  <div class="title">Bartender</div>
                  <div class="sub">Harpoon Brewery · Boston, MA</div>
                  <div class="sub">January 2018 – August 2018</div>
                  <div class="desc">
                    Supported taproom operations and guest experience; handled POS, workflow, and service standards
                    during peak hours.
                  </div>
                </div>
              </div>
            </div>

          </div>
        </div>
      </div>

    </section>
  </section>

  <!-- Accordion logic: one-open + animated panels (no auto-scroll) -->
  <script type="module">
    const accRoot = document.querySelector('.accordion');
    if (!accRoot) throw new Error('Accordion root not found');

    const items   = Array.from(accRoot.querySelectorAll('.acc'));
    const buttons = items.map(i => i.querySelector('.acc-summary'));
    const panels  = items.map(i => i.querySelector('.acc-panel'));

    function openPanel(panel) {
      panel.hidden = false;
      panel.style.display = 'block';
      const startHeight = panel.offsetHeight;
      const target = panel.scrollHeight;
      panel.style.height = startHeight + 'px';
      panel.getBoundingClientRect();
      panel.style.height = target + 'px';
      panel.classList.add('animating');
      panel.addEventListener('transitionend', function done(ev){
        if (ev.propertyName !== 'height') return;
        panel.removeEventListener('transitionend', done);
        panel.classList.remove('animating');
        panel.style.height = 'auto';
      });
    }

    function closePanel(panel) {
      const current = panel.offsetHeight;
      panel.style.height = current + 'px';
      panel.getBoundingClientRect();
      panel.style.height = '0px';
      panel.classList.add('animating');
      panel.addEventListener('transitionend', function done(ev){
        if (ev.propertyName !== 'height') return;
        panel.removeEventListener('transitionend', done);
        panel.classList.remove('animating');
        panel.style.display = 'none';
        panel.hidden = true;
      });
    }

    let openIndex = -1;

    function setOpen(idx) {
      items.forEach((_, i) => {
        const shouldOpen = i === idx;
        buttons[i].setAttribute('aria-expanded', String(shouldOpen));
        const p = panels[i];
        if (shouldOpen) {
          if (p.hidden || p.style.display === 'none' || p.style.height === '0px') openPanel(p);
        } else {
          if (!p.hidden && p.style.display !== 'none' && p.style.height !== '0px') closePanel(p);
        }
      });
      openIndex = idx;
    }

    function activate(i, evt) {
      if (evt) evt.preventDefault();
      const next = (openIndex === i) ? -1 : i;
      setOpen(next);
    }

    buttons.forEach((btn, i) => {
      btn.addEventListener('click', (e) => activate(i, e));
      btn.addEventListener('keydown', (e) => {
        if (e.key === ' ' || e.key === 'Enter') activate(i, e);
      });
    });

    // Initialize collapsed
    panels.forEach(p => { p.style.display = 'none'; p.hidden = true; p.style.height = '0px'; });
    openIndex = -1;
  </script>

  <style>
    .about-wrap{display:grid;gap:12px;justify-items:start;align-items:start;margin-inline:auto}
    .spacer-md{ height: clamp(24px, 6vh, 64px); width: 100%; }

    .accordion{
      display:grid; gap:12px; justify-items:start;
      width:100%; margin:0;
    }
    .acc{ width:100%; background:transparent; }

    .acc-summary{
      width:100%;
      display:flex;
      align-items:center;
      gap:0.6rem;
      padding:6px 0;
      cursor:pointer;
      user-select:none;
      color:var(--muted);
      background:none;
      border:0;
    }

    .acc-toggle{
      display:flex;
      align-items:center;
      justify-content:center;
      width:1.25rem;
      font-size:calc(var(--h1-size) * 0.8);
      font-weight:1000;
      line-height:1;
      color:var(--text);
      text-align:center;
    }
    .acc-toggle::before{ content:'+'; }
    .acc-summary[aria-expanded="true"] .acc-toggle::before{ content:'–'; }

    .acc-title{
      margin:0;
      font-size:var(--h1-size);
      font-weight:1000;
      letter-spacing:-0.01em;
      line-height:calc(var(--line) - .15);
      color:var(--text);
    }

    .acc-panel{
      padding-left: calc(1.25rem + 0.6rem);
      margin:0;
      display:block;
      height:0;
      overflow:hidden;
      overflow-anchor:none;
      transition: height 220ms ease;
      font-size:var(--body-size);
      line-height:1.45;
      color:var(--text);
    }

    .block, .list{ width:100%; margin:6px 0 0; }

    /* Education */
    .edu{ display:grid; gap:14px; }
    .edu-item{ display:grid; gap:6px; padding:10px 0; }
    .edu-top{ display:flex; align-items:baseline; justify-content:space-between; gap:12px; }
    .edu-school{ font-weight:700; font-size: clamp(16px, 1.7vw, 18px); }
    .edu-dates{ color:var(--muted); font-weight:600; font-size: clamp(14px, 1.4vw, 16px); white-space:nowrap; }
    .edu-sub{ color:var(--text); opacity:.9; font-weight:600; }

    /* Bullets */
    .bullets{ margin: 6px 0 0; padding-left: 1.1rem; }
    .bullets li{ margin: 6px 0; }

    /* Skills chips */
    .chips{
      display:flex;
      flex-wrap:wrap;
      gap:10px;
      padding: 6px 0 2px;
    }
    .chip{
      display:inline-flex;
      align-items:center;
      padding: .45rem .65rem;
      border-radius: 999px;
      border: 1px solid rgba(127,127,127,.35);
      background: rgba(127,127,127,.08);
      font-weight: 600;
      font-size: clamp(13px, 1.25vw, 15px);
      line-height: 1;
      white-space: nowrap;
    }

    /* Work list */
    .list{display:grid;gap:14px}
    .row{ text-decoration:none; color:inherit; }
    .row-line{display:flex;align-items:flex-start;gap:.85rem}
    .row-line.no-logo{ gap: 0; }
    .meta{display:grid;gap:.2rem}
    .title{
      font-weight: 650;
      font-size: clamp(16px, 1.6vw, 18px);
      letter-spacing: 0.005em;
      line-height: 1.15;
      color: var(--text);
    }
    .sub{
      color:inherit;
      font-weight: 550;
      font-size: clamp(14px, 1.35vw, 16px);
      opacity: .9;
    }
    .desc{
      margin-top: 6px;
      color: var(--text);
      opacity: .92;
      font-size: clamp(14px, 1.35vw, 16px);
      line-height: 1.55;
    }

    /* Hover dim behavior (keeps your existing feel) */
    .dim-others:hover .hoverable{ filter: grayscale(1) brightness(.85); opacity:.65; transition: filter 160ms ease, opacity 160ms ease; }
    .dim-others .hoverable:hover{ filter:none; opacity:1; }
  </style>
</Layout>
