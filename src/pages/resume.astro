---
import Layout from "../layouts/Layout.astro";
const RESUME_URL = "/resume/IPPOLITO_Resume_3D%20Architectural%20Visualizer.pdf";
---

<Layout title="Resume" description="Background, experience, and interests.">
  <section class="content resume-wrap">
    <div class="spacer-md" aria-hidden="true"></div>

    <!-- Accordion -->
    <section class="accordion" aria-label="Resume sections">
      <!-- EDUCATION -->
      <div class="acc" id="acc-education">
        <button class="acc-summary" type="button" aria-expanded="false" aria-controls="panel-education" id="btn-education">
          <span class="acc-toggle" aria-hidden="true"></span>
          <span class="acc-title">Education</span>
        </button>

        <div class="acc-panel" id="panel-education" role="region" aria-labelledby="btn-education">
          <div class="nested" role="list">
            <!-- UW -->
            <div class="nitem" role="listitem">
              <button class="nsummary" type="button" aria-expanded="false" aria-controls="n-edu-uw" id="nbtn-edu-uw">
                <span class="ncaret" aria-hidden="true"></span>
                <span class="nhead">University of Washington · College of Built Environments</span>
              </button>

              <div class="npanel" id="n-edu-uw" role="region" aria-labelledby="nbtn-edu-uw">
                <div class="detail">
                  <div class="detail-top">2021–2024</div>
                  <!-- No bullets under year -->
                  <p class="detail-text">Master of Architecture</p>
                  <p class="detail-text">Certificate of Historic Preservation</p>
                </div>
              </div>
            </div>

            <!-- BU -->
            <div class="nitem" role="listitem">
              <button class="nsummary" type="button" aria-expanded="false" aria-controls="n-edu-bu" id="nbtn-edu-bu">
                <span class="ncaret" aria-hidden="true"></span>
                <span class="nhead">Boston University · College of Fine Arts</span>
              </button>

              <div class="npanel" id="n-edu-bu" role="region" aria-labelledby="nbtn-edu-bu">
                <div class="detail">
                  <div class="detail-top">2013–2017</div>
                  <!-- No bullets under year -->
                  <p class="detail-text">Bachelors of Fine Arts in Graphic Design</p>
                  <p class="detail-text">Cum Laude</p>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>

      <!-- WORK -->
      <div class="acc" id="acc-work">
        <button class="acc-summary" type="button" aria-expanded="false" aria-controls="panel-work" id="btn-work">
          <span class="acc-toggle" aria-hidden="true"></span>
          <span class="acc-title">Work</span>
        </button>

        <div class="acc-panel" id="panel-work" role="region" aria-labelledby="btn-work">
          <div class="nested" role="list">
            <div class="nitem" role="listitem">
              <button class="nsummary" type="button" aria-expanded="false" aria-controls="n-work-mh" id="nbtn-work-mh">
                <span class="ncaret" aria-hidden="true"></span>
                <span class="nhead">Architecture Intern / Project Architect <span class="sep">|</span> Miller Hull · Seattle, WA</span>
              </button>
              <div class="npanel" id="n-work-mh" role="region" aria-labelledby="nbtn-work-mh">
                <div class="detail">
                  <div class="detail-top">June 2022 – September 2023</div>
                  <p class="detail-text">
                    Initially hired as a summer architecture intern; responsibilities included rendering development,
                    architectural graphics for awards submissions, geographic and historic site research, and physical model making.
                    Hired as a regular employee upon completion of the internship and continued part-time through the school year.
                    In summer 2023, contracted to Olson Kundig (remained a Miller Hull employee), focusing on renderings, site/material research,
                    presentation design, and floor plan schemes. Due to contract agreements, rendering examples from Olson Kundig work are not available.
                  </p>
                </div>
              </div>
            </div>

            <div class="nitem" role="listitem">
              <button class="nsummary" type="button" aria-expanded="false" aria-controls="n-work-uw" id="nbtn-work-uw">
                <span class="ncaret" aria-hidden="true"></span>
                <span class="nhead">ARCH 151 Reader / Grader <span class="sep">|</span> University of Washington · Seattle, WA</span>
              </button>
              <div class="npanel" id="n-work-uw" role="region" aria-labelledby="nbtn-work-uw">
                <div class="detail">
                  <div class="detail-top">March 2024 – June 2024</div>
                  <p class="detail-text">
                    Supported course instruction through assignment evaluation and feedback; coordinated grading workflow and maintained
                    consistency across rubric-based assessments.
                  </p>
                </div>
              </div>
            </div>

            <div class="nitem" role="listitem">
              <button class="nsummary" type="button" aria-expanded="false" aria-controls="n-work-is" id="nbtn-work-is">
                <span class="ncaret" aria-hidden="true"></span>
                <span class="nhead">Project Coordinator <span class="sep">|</span> Interior Showplace · Honolulu, HI</span>
              </button>
              <div class="npanel" id="n-work-is" role="region" aria-labelledby="nbtn-work-is">
                <div class="detail">
                  <div class="detail-top">March 2021 – August 2021</div>
                  <p class="detail-text">
                    Coordinated private and government bids involving interior design services and furniture purchases across Oahu, Maui, Hawai’i, and Kaua’i.
                    Supported initial design services through furniture research and quote-building with designers; placed and tracked orders; coordinated delivery
                    and scheduled installation. Projects ranged from $1,000 to $700,000. Designed presentation templates and marketing materials to support the sales team.
                  </p>
                </div>
              </div>
            </div>

            <div class="nitem" role="listitem">
              <button class="nsummary" type="button" aria-expanded="false" aria-controls="n-work-novus" id="nbtn-work-novus">
                <span class="ncaret" aria-hidden="true"></span>
                <span class="nhead">Marketing and Graphics Coordinator <span class="sep">|</span> Novus Architects, Inc. · Charleston, SC</span>
              </button>
              <div class="npanel" id="n-work-novus" role="region" aria-labelledby="nbtn-work-novus">
                <div class="detail">
                  <div class="detail-top">September 2019 – February 2021</div>
                  <p class="detail-text">
                    Supported Business Development with research and evaluation of RFPs/RFQs and created proposal submissions for public and private bids.
                    Designed print and digital marketing materials (brochures, infographics, postcards, presentations). Coordinated project photography, developed
                    social media campaigns, updated the website, and supported environmental graphics efforts. Reported to the Director of Business Development;
                    led weekly marketing meetings, presented updates in bi-weekly all-office meetings, and trained/managed the marketing intern.
                  </p>
                </div>
              </div>
            </div>

            <div class="nitem" role="listitem">
              <button class="nsummary" type="button" aria-expanded="false" aria-controls="n-work-mums" id="nbtn-work-mums">
                <span class="ncaret" aria-hidden="true"></span>
                <span class="nhead">Marketing Coordinator <span class="sep">|</span> Mum’s Place · Monterey, CA</span>
              </button>
              <div class="npanel" id="n-work-mums" role="region" aria-labelledby="nbtn-work-mums">
                <div class="detail">
                  <div class="detail-top">September 2018 – July 2019</div>
                  <p class="detail-text">
                    Focused on website optimization, social media maintenance, branding development, and advertising coordination.
                    Occasionally supported interior designers with finish selections, proposal coordination, and client communications.
                  </p>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>

      <!-- SKILLS -->
      <div class="acc" id="acc-skills">
        <button class="acc-summary" type="button" aria-expanded="false" aria-controls="panel-skills" id="btn-skills">
          <span class="acc-toggle" aria-hidden="true"></span>
          <span class="acc-title">Skills</span>
        </button>
        <div class="acc-panel" id="panel-skills" role="region" aria-labelledby="btn-skills">
          <div class="block">
            <div class="chips" aria-label="Skills list">
              <span class="chip">Adobe Suite</span>
              <span class="chip">Rhino 7</span>
              <span class="chip">Revit</span>
              <span class="chip">Enscape / Lumion</span>
              <span class="chip">Microsoft Office</span>
              <span class="chip">3D printing</span>
              <span class="chip">Laser Cutting</span>
              <span class="chip">CNC Milling</span>
              <span class="chip">Woodworking</span>
              <span class="chip">TIG Welding</span>
              <span class="chip">Knitting</span>
              <span class="chip">Sewing</span>
              <span class="chip">Needlepoint</span>
            </div>
          </div>
        </div>
      </div>

      <!-- ACHIEVEMENTS / AWARDS -->
      <div class="acc" id="acc-achievements">
        <button class="acc-summary" type="button" aria-expanded="false" aria-controls="panel-achievements" id="btn-achievements">
          <span class="acc-toggle" aria-hidden="true"></span>
          <span class="acc-title">Achievements / Awards</span>
        </button>
        <div class="acc-panel" id="panel-achievements" role="region" aria-labelledby="btn-achievements">
          <div class="block">
            <ul class="bullets light">
              <li>Secretary, University of Washington College of Built Environments Happy Hour</li>
              <li>Youths in the Built Environments — Volunteer</li>
              <li>Captain, Boston University Lightweight Women’s Rowing</li>
              <li>BU Athletics Leadership Academy</li>
            </ul>
          </div>
        </div>
      </div>

      <!-- FILL IN THE GAPS -->
      <div class="acc" id="acc-gaps">
        <button class="acc-summary" type="button" aria-expanded="false" aria-controls="panel-gaps" id="btn-gaps">
          <span class="acc-toggle" aria-hidden="true"></span>
          <span class="acc-title">Fill in the Gaps</span>
        </button>

        <div class="acc-panel" id="panel-gaps" role="region" aria-labelledby="btn-gaps">
          <div class="nested" role="list">
            <div class="nitem" role="listitem">
              <button class="nsummary" type="button" aria-expanded="false" aria-controls="n-gap-metier" id="nbtn-gap-metier">
                <span class="ncaret" aria-hidden="true"></span>
                <span class="nhead">Bartender <span class="sep">|</span> Métier Brewing Co. · Seattle, WA</span>
              </button>
              <div class="npanel" id="n-gap-metier" role="region" aria-labelledby="nbtn-gap-metier">
                <div class="detail">
                  <div class="detail-top">April 2022 – January 2024</div>
                  <p class="detail-text">Managed high-volume service, maintained bar operations and inventory, and supported a welcoming guest experience.</p>
                </div>
              </div>
            </div>

            <div class="nitem" role="listitem">
              <button class="nsummary" type="button" aria-expanded="false" aria-controls="n-gap-optimism" id="nbtn-gap-optimism">
                <span class="ncaret" aria-hidden="true"></span>
                <span class="nhead">Bartender <span class="sep">|</span> Optimism Brewery · Seattle, WA</span>
              </button>
              <div class="npanel" id="n-gap-optimism" role="region" aria-labelledby="nbtn-gap-optimism">
                <div class="detail">
                  <div class="detail-top">September 2021 – April 2022</div>
                  <p class="detail-text">Provided fast-paced service and coordinated day-to-day bar setup/close while maintaining quality and consistency.</p>
                </div>
              </div>
            </div>

            <div class="nitem" role="listitem">
              <button class="nsummary" type="button" aria-expanded="false" aria-controls="n-gap-island" id="nbtn-gap-island">
                <span class="ncaret" aria-hidden="true"></span>
                <span class="nhead">Server <span class="sep">|</span> Island Vintage Wine Bar · Waikiki, HI</span>
              </button>
              <div class="npanel" id="n-gap-island" role="region" aria-labelledby="nbtn-gap-island">
                <div class="detail">
                  <div class="detail-top">April 2021 – August 2021</div>
                  <p class="detail-text">Delivered attentive table service in a high-traffic environment; coordinated timing and communication between guests, bar, and kitchen.</p>
                </div>
              </div>
            </div>

            <div class="nitem" role="listitem">
              <button class="nsummary" type="button" aria-expanded="false" aria-controls="n-gap-harpoon" id="nbtn-gap-harpoon">
                <span class="ncaret" aria-hidden="true"></span>
                <span class="nhead">Bartender <span class="sep">|</span> Harpoon Brewery · Boston, MA</span>
              </button>
              <div class="npanel" id="n-gap-harpoon" role="region" aria-labelledby="nbtn-gap-harpoon">
                <div class="detail">
                  <div class="detail-top">January 2018 – August 2018</div>
                  <p class="detail-text">Supported taproom operations and guest experience; handled POS, workflow, and service standards during peak hours.</p>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>

      <!-- PDF (link, last) -->
      <div class="acc acc-link" id="acc-pdf">
        <a class="acc-summary acc-linkbtn" href={RESUME_URL} target="_blank" rel="noopener" aria-label="Open resume PDF">
          <span class="acc-toggle" aria-hidden="true"></span>
          <span class="acc-title">
            Pdf <span class="ext" aria-hidden="true">↗</span>
          </span>
        </a>
      </div>
    </section>
  </section>

  <script type="module">
    // Main accordion (one-open)
    const accRoot = document.querySelector('.accordion');
    if (!accRoot) throw new Error('Accordion root not found');

    const accItems   = Array.from(accRoot.querySelectorAll(':scope > .acc')).filter(x => !x.classList.contains('acc-link'));
    const accButtons = accItems.map(i => i.querySelector(':scope > .acc-summary'));
    const accPanels  = accItems.map(i => i.querySelector(':scope > .acc-panel'));

    function openPanel(panel) {
      panel.hidden = false;
      panel.style.display = 'block';
      const startHeight = panel.offsetHeight;
      const target = panel.scrollHeight;
      panel.style.height = startHeight + 'px';
      panel.getBoundingClientRect();
      panel.style.height = target + 'px';
      panel.classList.add('animating');
      panel.addEventListener('transitionend', function done(ev){
        if (ev.propertyName !== 'height') return;
        panel.removeEventListener('transitionend', done);
        panel.classList.remove('animating');
        panel.style.height = 'auto';
      });
    }

    function closePanel(panel) {
      const current = panel.offsetHeight;
      panel.style.height = current + 'px';
      panel.getBoundingClientRect();
      panel.style.height = '0px';
      panel.classList.add('animating');
      panel.addEventListener('transitionend', function done(ev){
        if (ev.propertyName !== 'height') return;
        panel.removeEventListener('transitionend', done);
        panel.classList.remove('animating');
        panel.style.display = 'none';
        panel.hidden = true;
      });
    }

    let openIndex = -1;

    function setOpen(idx) {
      accItems.forEach((_, i) => {
        const shouldOpen = i === idx;
        accButtons[i].setAttribute('aria-expanded', String(shouldOpen));
        const p = accPanels[i];
        if (shouldOpen) {
          if (p.hidden || p.style.display === 'none' || p.style.height === '0px') openPanel(p);
        } else {
          if (!p.hidden && p.style.display !== 'none' && p.style.height !== '0px') closePanel(p);
        }
      });
      openIndex = idx;
    }

    function activate(i, evt) {
      if (evt) evt.preventDefault();
      const next = (openIndex === i) ? -1 : i;
      setOpen(next);
    }

    // Initialize collapsed
    accPanels.forEach(p => { p.style.display = 'none'; p.hidden = true; p.style.height = '0px'; });
    openIndex = -1;

    accButtons.forEach((btn, i) => {
      btn.addEventListener('click', (e) => activate(i, e));
      btn.addEventListener('keydown', (e) => {
        if (e.key === ' ' || e.key === 'Enter') activate(i, e);
      });
    });

    // Nested accordions (one-open per section)
    function initNested(root) {
      const nitems = Array.from(root.querySelectorAll('.nitem'));
      const nbtns  = nitems.map(it => it.querySelector('.nsummary'));
      const npanels= nitems.map(it => it.querySelector('.npanel'));

      npanels.forEach(p => { p.style.display = 'none'; p.hidden = true; p.style.height = '0px'; });

      let openN = -1;

      const openNPanel = (panel) => {
        panel.hidden = false;
        panel.style.display = 'block';
        const startHeight = panel.offsetHeight;
        const target = panel.scrollHeight;
        panel.style.height = startHeight + 'px';
        panel.getBoundingClientRect();
        panel.style.height = target + 'px';
        panel.classList.add('animating');
        panel.addEventListener('transitionend', function done(ev){
          if (ev.propertyName !== 'height') return;
          panel.removeEventListener('transitionend', done);
          panel.classList.remove('animating');
          panel.style.height = 'auto';
        });
      };

      const closeNPanel = (panel) => {
        const current = panel.offsetHeight;
        panel.style.height = current + 'px';
        panel.getBoundingClientRect();
        panel.style.height = '0px';
        panel.classList.add('animating');
        panel.addEventListener('transitionend', function done(ev){
          if (ev.propertyName !== 'height') return;
          panel.removeEventListener('transitionend', done);
          panel.classList.remove('animating');
          panel.style.display = 'none';
          panel.hidden = true;
        });
      };

      const setNOpen = (idx) => {
        nitems.forEach((_, i) => {
          const shouldOpen = i === idx;
          nbtns[i].setAttribute('aria-expanded', String(shouldOpen));
          const p = npanels[i];
          if (shouldOpen) {
            if (p.hidden || p.style.display === 'none' || p.style.height === '0px') openNPanel(p);
          } else {
            if (!p.hidden && p.style.display !== 'none' && p.style.height !== '0px') closeNPanel(p);
          }
        });
        openN = idx;
      };

      nbtns.forEach((btn, i) => {
        btn.addEventListener('click', (e) => {
          e.preventDefault();
          const next = (openN === i) ? -1 : i;
          setNOpen(next);
        });
        btn.addEventListener('keydown', (e) => {
          if (e.key === ' ' || e.key === 'Enter') {
            e.preventDefault();
            const next = (openN === i) ? -1 : i;
            setNOpen(next);
          }
        });
      });
    }

    accPanels.forEach(p => {
      const nested = p.querySelector('.nested');
      if (nested) initNested(nested);
    });
  </script>

  <style>
    /* Match About page: centered 60% text block feel */
    .resume-wrap{
      width: 100%;
      margin-inline: auto;
      max-width: min(60vw, 860px);
    }
    @media (max-width: 900px){ .resume-wrap{ max-width: 100%; } }

    .spacer-md{ height: clamp(24px, 6vh, 64px); width: 100%; }

    /* Main accordion */
    .accordion{ display:grid; gap:12px; width:100%; margin:0; }
    .acc{ width:100%; background:transparent; }

    .acc-summary{
      width:100%;
      display:flex;
      align-items:center;
      gap:0.6rem;
      padding:6px 0;
      cursor:pointer;
      user-select:none;
      background:none;
      border:0;
      text-decoration:none;
    }
    .acc-toggle{
      display:flex;
      align-items:center;
      justify-content:center;
      width:1.25rem;
      font-size:calc(var(--h1-size) * 0.8);
      font-weight:1000;
      line-height:1;
      color:var(--text);
      text-align:center;
    }
    .acc-toggle::before{ content:'+'; }
    .acc-summary[aria-expanded="true"] .acc-toggle::before{ content:'–'; }

    .acc-title{
      margin:0;
      font-size:var(--h1-size);
      font-weight:1000;
      letter-spacing:-0.01em;
      line-height:calc(var(--line) - .15);
      color:var(--text);
    }
    .ext{ opacity:.6; font-weight:900; margin-left:.25rem; font-size: .85em; }

    .acc-panel{
      padding-left: calc(1.25rem + 0.6rem);
      margin:0;
      display:block;
      height:0;
      overflow:hidden;
      overflow-anchor:none;
      transition: height 220ms ease;
      font-size:var(--body-size);
      line-height: var(--line);
      color:var(--text);
    }

    /* PDF link row (keeps + icon consistent) */
    .acc-linkbtn:hover .acc-title{ text-decoration: underline; text-decoration-thickness: 2px; text-underline-offset: 4px; }
    .acc-link .acc-toggle::before{ content:'+'; }

    /* Nested accordions: main bullets match About text size */
    .nested{ width:100%; margin-top:6px; display:grid; gap:10px; }
    .nitem{ width:100%; }

    .nsummary{
      width:100%;
      display:flex;
      align-items:center;
      gap:.55rem;
      padding:6px 0;
      background:none;
      border:0;
      cursor:pointer;
      text-align:left;
      color:var(--text);
      font-weight: 800;          /* plus bolded */
      font-size: var(--body-size); /* same size as about-text */
      line-height: var(--line);
    }

    .ncaret{
      width:1rem;
      display:flex;
      align-items:center;
      justify-content:center;
      font-weight:1000;
      line-height:1;
      opacity:.9;
    }
    .ncaret::before{ content:'›'; transform: translateY(-0.5px); display:inline-block; transition: transform 160ms ease; }
    .nsummary[aria-expanded="true"] .ncaret::before{ transform: rotate(90deg) translateX(1px); }

    .sep{ opacity:.45; padding: 0 .15rem; }

    .npanel{
      margin:0;
      display:block;
      height:0;
      overflow:hidden;
      transition: height 200ms ease;
      padding-left: calc(1rem + .55rem);
    }
    .npanel.animating{ will-change: height; }

    /* Keep everything inside panels the same text size for now */
    .detail{ margin-top: 6px; }
    .detail-top{
      font-weight: 600;
      opacity: .85;
      font-size: var(--body-size);
      line-height: var(--line);
      margin-bottom: 6px;
    }
    .detail-text{
      margin: 0 0 6px 0;
      font-weight: 400;
      opacity: .92;
      font-size: var(--body-size);
      line-height: var(--line);
    }

    .bullets{
      margin: 6px 0 0;
      padding-left: 1.1rem;
      font-weight: 400;
      opacity: .92;
      font-size: var(--body-size);
      line-height: var(--line);
    }
    .bullets.light li{ margin: 6px 0; }

    /* Skills chips */
    .chips{
      display:flex;
      flex-wrap:wrap;
      gap:10px;
      padding: 6px 0 2px;
    }
    .chip{
      display:inline-flex;
      align-items:center;
      padding: .45rem .65rem;
      border-radius: 999px;
      border: 1px solid rgba(127,127,127,.35);
      background: rgba(127,127,127,.08);
      font-weight: 600;
      font-size: clamp(13px, 1.25vw, 15px);
      line-height: 1;
      white-space: nowrap;
    }
  </style>
</Layout>
