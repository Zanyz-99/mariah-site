---
import Layout from "../layouts/Layout.astro";
---

<Layout title="About Me" description="About Me">
  <section class="content about-wrap">
    <div class="spacer-md" aria-hidden="true"></div>

    <p class="about-text">
      I find inspiration in ordinary architecture and have a deep appreciation for the cultural heritage embedded in historic places. While I hope to work to preserve our shared history, I also hope to utilize existing architectural resources and design spaces that further community opportunity and connection.
    </p>
  </section>

  <style>
    /* Page wrapper: centered 80% */
    .about-wrap {
      width: min(80%, 1200px); /* optional max-width safety */
      margin-inline: auto;
    }

    .spacer-md {
      height: clamp(24px, 6vh, 64px);
      width: 100%;
    }

    /* Text block */
    .about-text {
      font-size: var(--body-size);
      line-height: var(--line);
      color: var(--text);
      max-width: 65ch; /* ideal reading width */
    }

    /* Mobile */
    @media (max-width: 768px) {
      .about-wrap {
        width: 90%;
      }
    }
  </style>
</Layout>
