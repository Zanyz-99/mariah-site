---
import Layout from "../layouts/Layout.astro";

const slides = [
  "/projects/02 Project Two.png",
  "/projects/03 Project Three.png",
  "/projects/04 Project Four.png",
  "/projects/05 Project Five.png",
  "/projects/06 Project Six.png",
];
---

<Layout title="Home" description="">
  <section class="home">
    <!-- Full-bleed row -->
    <div class="full-bleed">
      <div class="carousel" id="projects">
        <div class="viewport" id="viewport" tabindex="0" aria-label="Projects carousel">
          <ul class="track" id="track" aria-live="polite">
            <!-- Only 2 cards: current + next -->
            <li class="card is-active"><img id="img-cur" alt="Current project" decoding="async" /></li>
            <li class="card"><img id="img-next" alt="Next project" decoding="async" /></li>
          </ul>
        </div>

        <!-- dots pinned to normal content width -->
        <div class="inner">
          <div class="dots" id="dots" role="tablist" aria-label="Project slide navigation">
            {slides.map((_, i) => (
              <button class="dot" type="button" data-index={i} aria-label={`Go to slide ${i + 1}`} />
            ))}
          </div>
        </div>
      </div>
    </div>
  </section>

  <script define:vars={{ slides }}>
    const viewport = document.getElementById("viewport");
    const track = document.getElementById("track");
    const dots = document.querySelectorAll("#dots .dot");

    const imgCur  = document.getElementById("img-cur");
    const imgNext = document.getElementById("img-next");

    const cards = Array.from(track.querySelectorAll(".card"));

    const n = slides.length;
    const mod = (x) => (x % n + n) % n;

    let index = 0;
    let animating = false;

    const setDots = () => {
      dots.forEach((d, i) => {
        if (i === index) d.setAttribute("data-active", "true");
        else d.removeAttribute("data-active");
        d.setAttribute("aria-selected", i === index ? "true" : "false");
      });
    };

    const setImages = () => {
      imgCur.src = slides[index];
      imgNext.src = slides[mod(index + 1)];
      imgCur.loading = "eager";
      imgNext.loading = "lazy";
    };

    const getGap = () => {
      const cs = getComputedStyle(track);
      return parseFloat(cs.columnGap || cs.gap || "0") || 0;
    };

    const cardWidth = () => cards[0].getBoundingClientRect().width;

    // ✅ Center the CURRENT card within the viewport
    const centerOffset = () => {
      const pL = parseFloat(getComputedStyle(viewport).paddingLeft || "0") || 0;
      const pR = parseFloat(getComputedStyle(viewport).paddingRight || "0") || 0;
      const innerW = (viewport.clientWidth - pL - pR) || 1;

      const w = cardWidth();
      const innerCenter = innerW / 2;

      const curCenterInTrack = w / 2; // current starts at x=0 in track
      return (pL + innerCenter) - curCenterInTrack;
    };

    // One step moves left by (current width + gap) so "next" becomes current
    const stepSize = () => cardWidth() + getGap();

    const snapCenter = () => {
      track.style.transition = "none";
      track.style.transform = `translateX(${centerOffset()}px)`;
      track.offsetHeight;
    };

    const stepNext = () => {
      if (animating) return;
      animating = true;

      const base = centerOffset();
      const dist = stepSize();

      track.style.transition = "transform 320ms ease";
      track.style.transform = `translateX(${base - dist}px)`;

      const onDone = () => {
        track.removeEventListener("transitionend", onDone);

        index = mod(index + 1);
        setImages();
        setDots();
        snapCenter();

        animating = false;
      };

      track.addEventListener("transitionend", onDone);
    };

    const goTo = (target) => {
      target = mod(target);
      if (target === index || animating) return;

      // for non-adjacent, just jump (clean + simple)
      index = target;
      setImages();
      setDots();
      snapCenter();
    };

    dots.forEach((dot) => {
      dot.addEventListener("click", () => {
        const t = Number(dot.getAttribute("data-index"));
        goTo(t);
      });
    });

    // Wheel/trackpad -> next only (as requested)
    let wheelLock = false;
    viewport.addEventListener(
      "wheel",
      (e) => {
        e.preventDefault();
        if (wheelLock || animating) return;
        wheelLock = true;

        stepNext();

        setTimeout(() => (wheelLock = false), 340);
      },
      { passive: false }
    );

    // Drag / swipe: only left swipe advances
    let startX = 0;
    let dragging = false;

    viewport.addEventListener("pointerdown", (e) => {
      if (animating) return;
      dragging = true;
      startX = e.clientX;
      viewport.setPointerCapture?.(e.pointerId);
    });

    viewport.addEventListener("pointerup", (e) => {
      if (!dragging || animating) return;
      dragging = false;

      const dx = e.clientX - startX;
      const threshold = Math.min(90, viewport.clientWidth * 0.14);

      if (dx < -threshold) stepNext();
    });

    viewport.addEventListener("pointercancel", () => {
      dragging = false;
    });

    // Keyboard: right arrow advances
    viewport.addEventListener("keydown", (e) => {
      if (animating) return;
      if (e.key === "ArrowRight") stepNext();
    });

    window.addEventListener("resize", () => snapCenter());

    // Init
    setImages();
    setDots();
    snapCenter();
  </script>

  <style>
    .home{
      display: grid;
      gap: 16px;
      width: 100%;
      padding-top: clamp(12px, 3vh, 28px);
      padding-bottom: clamp(18px, 4vh, 44px);
    }

    .carousel{ width: 100%; }

    .viewport{
      width: 100%;
      overflow: hidden;
      outline: none;
      touch-action: pan-y;
      padding-inline: clamp(10px, 2vw, 18px);
      box-sizing: border-box;
    }

    .track{
      display: flex;
      align-items: center;
      gap: clamp(10px, 2vw, 18px);
      padding: 6px 0;
      will-change: transform;
    }

    .card{
      flex: 0 0 auto;

      /* set card big so next only "peeks" */
      width: clamp(320px, 78vw, 980px);

      border-radius: 16px;
      overflow: hidden;
      aspect-ratio: 16 / 9;

      background: var(--card);
      border: 1px solid var(--border);

      transform: scale(0.94);
      opacity: 0.85;
      transition: transform 200ms ease, opacity 200ms ease;
    }

    .card.is-active{
      transform: scale(1);
      opacity: 1;
    }

    .card img{
      width: 100%;
      height: 100%;
      display: block;
      object-fit: contain;
      object-position: center;
      user-select: none;
      -webkit-user-drag: none;
    }

    .dots{
      margin-top: 0.9rem;
      display: flex;
      justify-content: center;
      gap: 10px;
      width: 100%;
    }

    .dot{
      width: 10px;
      height: 10px;
      border-radius: 999px;
      border: 0;
      background: currentColor;
      opacity: 0.35;
      cursor: pointer;
      transition: transform 160ms ease, opacity 160ms ease;
      position: relative;
    }

    .dot[data-active="true"]{
      opacity: 1;
      transform: scale(1.35);
    }

    .dot::before{
      content: "";
      position: absolute;
      inset: -10px;
    }
  </style>
</Layout>
