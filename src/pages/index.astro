---
import Layout from "../layouts/Layout.astro";

const slides = [
  "/projects/02 Project Two.png",
  "/projects/03 Project Three.png",
  "/projects/04 Project Four.png",
  "/projects/05 Project Five.png",
  "/projects/06 Project Six.png",
];
---

<Layout title="Home" description="">
  <section class="home">
    <div class="carousel" id="projects">
      <div class="viewport" id="viewport" tabindex="0" aria-label="Projects carousel">
        <ul class="track" id="track" aria-live="polite">
          <!-- we only keep 3 cards in the DOM and swap their images -->
          <li class="card"><img id="img-prev" alt="Previous project" decoding="async" /></li>
          <li class="card is-active"><img id="img-cur" alt="Current project" decoding="async" /></li>
          <li class="card"><img id="img-next" alt="Next project" decoding="async" /></li>
        </ul>
      </div>

      <div class="dots" id="dots" role="tablist" aria-label="Project slide navigation">
        {slides.map((_, i) => (
          <button class="dot" type="button" data-index={i} aria-label={`Go to slide ${i + 1}`} />
        ))}
      </div>
    </div>
  </section>

  <script define:vars={{ slides }}>
    const viewport = document.getElementById("viewport");
    const track = document.getElementById("track");
    const dots = document.querySelectorAll("#dots .dot");

    const imgPrev = document.getElementById("img-prev");
    const imgCur  = document.getElementById("img-cur");
    const imgNext = document.getElementById("img-next");

    const cards = Array.from(track.querySelectorAll(".card"));

    const n = slides.length;
    const mod = (x) => (x % n + n) % n;

    let index = 0;
    let animating = false;

    const setDots = () => {
      dots.forEach((d, i) => {
        if (i === index) d.setAttribute("data-active", "true");
        else d.removeAttribute("data-active");
        d.setAttribute("aria-selected", i === index ? "true" : "false");
      });
    };

    const setCardImages = () => {
      const prev = mod(index - 1);
      const next = mod(index + 1);

      imgPrev.src = slides[prev];
      imgCur.src  = slides[index];
      imgNext.src = slides[next];

      imgCur.loading = "eager";
      imgPrev.loading = "lazy";
      imgNext.loading = "lazy";
    };

    // Calculate how far to translate for one step:
    // card width + gap (in px). This is what enables the "peek" layout.
    const stepSize = () => {
      const gap = parseFloat(getComputedStyle(track).columnGap || getComputedStyle(track).gap || "0") || 0;
      const card = cards[1]; // center card
      const w = card.getBoundingClientRect().width;
      return w + gap;
    };

    const snapCenter = () => {
      track.style.transition = "none";
      track.style.transform = "translateX(0px)";
      track.offsetHeight; // reflow
    };

    const step = (dir) => {
      if (animating) return;
      animating = true;

      // animate left/right by one card step
      track.style.transition = "transform 320ms ease";
      track.style.transform = `translateX(${dir === 1 ? -stepSize() : stepSize()}px)`;

      const onDone = () => {
        track.removeEventListener("transitionend", onDone);

        index = mod(index + dir);
        setCardImages();
        setDots();
        snapCenter();

        animating = false;
      };

      track.addEventListener("transitionend", onDone);
    };

    const goTo = (target) => {
      target = mod(target);
      if (target === index || animating) return;

      // adjacent? animate; otherwise jump instantly
      const forward = mod(target - index);
      const backward = mod(index - target);

      if (forward === 1) return step(1);
      if (backward === 1) return step(-1);

      index = target;
      setCardImages();
      setDots();
      snapCenter();
    };

    dots.forEach((dot) => {
      dot.addEventListener("click", () => {
        const t = Number(dot.getAttribute("data-index"));
        goTo(t);
      });
    });

    // Wheel paging
    let wheelLock = false;
    viewport.addEventListener(
      "wheel",
      (e) => {
        e.preventDefault();
        if (wheelLock || animating) return;
        wheelLock = true;

        const dominant = Math.abs(e.deltaY) >= Math.abs(e.deltaX) ? e.deltaY : e.deltaX;
        step(dominant > 0 ? 1 : -1);

        setTimeout(() => (wheelLock = false), 340);
      },
      { passive: false }
    );

    // Drag / swipe (snap on release)
    let startX = 0;
    let dragging = false;

    viewport.addEventListener("pointerdown", (e) => {
      if (animating) return;
      dragging = true;
      startX = e.clientX;
      viewport.setPointerCapture?.(e.pointerId);
    });

    viewport.addEventListener("pointerup", (e) => {
      if (!dragging || animating) return;
      dragging = false;

      const dx = e.clientX - startX;
      const threshold = Math.min(90, viewport.clientWidth * 0.14);

      if (dx < -threshold) step(1);
      else if (dx > threshold) step(-1);
    });

    viewport.addEventListener("pointercancel", () => {
      dragging = false;
    });

    // Keyboard (when focused)
    viewport.addEventListener("keydown", (e) => {
      if (animating) return;
      if (e.key === "ArrowRight") step(1);
      if (e.key === "ArrowLeft") step(-1);
    });

    // If viewport size changes, just re-snap (so step size recalcs cleanly)
    window.addEventListener("resize", () => snapCenter());

    // Init
    setCardImages();
    setDots();
    snapCenter();
  </script>

  <style>
    .home{
      display: grid;
      gap: 16px;
      width: 100%;
      padding-top: clamp(12px, 3vh, 28px);
      padding-bottom: clamp(18px, 4vh, 44px);
    }

    .carousel{
      width: 100%;
    }

    .viewport{
      width: 100%;
      overflow: hidden;        /* keeps the row clean */
      outline: none;
      touch-action: pan-y;
    }

    /* Netflix-ish row: center card big, neighbors peek */
    .track{
      display: flex;
      align-items: center;
      gap: clamp(10px, 2vw, 18px);

      /* This gives room for the "peek" without needing scroll */
      padding: 6px 0;

      transform: translateX(0px);
      will-change: transform;
    }

    /* Card size controls how much peek you get:
       - 78vw on mobile (big card, small peek)
       - 70% on desktop (more peek)
    */
    .card{
      flex: 0 0 auto;
      width: clamp(280px, 78vw, 720px);
      max-width: 92%;
      border-radius: 16px;
      overflow: hidden;

      /* Consistent height without cropping: use aspect-ratio */
      aspect-ratio: 16 / 9;

      background: var(--card);
      border: 1px solid var(--border);

      /* subtle scale for depth */
      transform: scale(0.94);
      opacity: 0.85;
      transition: transform 200ms ease, opacity 200ms ease;
    }

    /* Center card emphasis */
    .card.is-active{
      transform: scale(1);
      opacity: 1;
    }

    /* Images: show full PNG, no crop */
    .card img{
      width: 100%;
      height: 100%;
      display: block;
      object-fit: contain;
      object-position: center;
      user-select: none;
      -webkit-user-drag: none;
    }

    /* Dots */
    .dots{
      margin-top: 0.9rem;
      display: flex;
      justify-content: center;
      gap: 10px;
      width: 100%;
    }

    .dot{
      width: 10px;
      height: 10px;
      border-radius: 999px;
      border: 0;
      background: currentColor;
      opacity: 0.35;
      cursor: pointer;
      transition: transform 160ms ease, opacity 160ms ease;
      position: relative;
    }

    .dot[data-active="true"]{
      opacity: 1;
      transform: scale(1.35);
    }

    .dot::before{
      content: "";
      position: absolute;
      inset: -10px;
    }

    /* Desktop: more "peek" */
    @media (min-width: 900px){
      .card{
        width: min(70%, 760px);
      }
    }
  </style>
</Layout>
